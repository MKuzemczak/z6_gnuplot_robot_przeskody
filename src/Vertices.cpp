#include <iostream>

#include "Vertices.hh"

using namespace std;
/*
ostream & operator << (ostream & out, const LineVertices l) // przeniesc deklaracje do *.cpp
{
    for (Wektor3D v : l)
    {
        out << v << endl;
    }
    
    return out;
}

ostream & operator << (ostream & out, const ShapeVertices s) // przeniesc deklaracje do *.cpp
{
    for (unsigned int i = 0; i < s.size() - 1; i++)
    {
        out << s[i] << "#\n\n";
    }

    out << s[s.size() - 1] << "\n\n";

    return out;
}
*/